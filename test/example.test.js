const chai = require('chai')
const expect = chai.expect

describe('Calculator', () => {
    describe('Addition', () => {
        it('1 should be 1', () => {
            const num = 1;
            expect(num).to.equal(1)
        })
    })
})