version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8
      - image: mongo:3.4.4
    working_directory: ~/build
    steps:
      - checkout
      - run: npm install
      - run: npm test
        
workflows:
  version: 2
  build_and_test:
    jobs:
      - build