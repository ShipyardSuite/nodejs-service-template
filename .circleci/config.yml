version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    working_directory: ~/build
    steps:
      - checkout
      - run:
          name: "install dependencies"
          command: "npm install"
      - run:
          name: "run tests"
          command: "npm test"
